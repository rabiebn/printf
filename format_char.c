#include "main.h"
/**
 * print_char - helper function that formats a single character
 * @out_str: pointer to the output string buffer
 * @c: the character to format
 * Return: the number of characters printed (always 1)
 */
int format_char(va_list args, params_t *params)
{
	*out_str = c;
	return (1);
}
